 let radius = readLine()
    const PI = Math.PI 
		//3.141592653589793 --javaScript 
		//3.14159265359 -- googled value
        
    let area = PI*Math.pow(radius,2)  //PI*(radius*radius)
    console.log(area)
    
    let perimeter = 2*PI*radius
    console.log(perimeter)